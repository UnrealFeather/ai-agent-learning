下面把 **阶段 3：Tool Calling（Day 29 - Day 42）**补成可以直接照着做的完整学习路线。

基于你当前已经完成的第二阶段（DeepSeek + FastAPI + 多轮对话 + 结构化输出），第三阶段目标是从：

```text
LLM 只负责回答
```

升级到：

```text
LLM 能决定调用哪个工具
→ 后端执行工具
→ 再把结果返回给模型
→ 模型生成最终回答
```

这才是真正进入 Agent 开发。

---

# 阶段 3 最终目标（Day 29 - Day 42）

完成后你要实现一个真正的 Agent：

用户：

> 帮我查订单 1001

系统执行：

```text
LLM 判断需要调用 query_order
↓
返回 tool_call
↓
后端执行 query_order()
↓
把工具结果传回模型
↓
模型生成自然语言回答
```

最终接口：

```text
POST /agent/tool-chat
```

支持：

```text
1. 查订单
2. 获取天气（模拟）
3. 面试题生成
4. 订单退款申请（模拟）
5. 多工具路由
6. 自动工具选择
7. 工具失败重试
8. 工具注册机制
```

---

# 第三阶段项目新增结构

完成后：

```text
app/
├── tools/
│   ├── base.py
│   ├── registry.py
│   ├── order_tools.py
│   ├── weather_tools.py
│   ├── interview_tools.py
│   └── refund_tools.py
├── services/
│   ├── tool_call_service.py
│   ├── tool_executor.py
│   └── tool_agent_service.py
├── schemas/
│   ├── tool.py
```

---

# Day 29：理解 Tool Calling 原理

---

## 学习目标

彻底理解 Agent 工具调用闭环。

你必须理解下面流程：

```text
用户输入
↓
LLM 推理
↓
决定是否调用工具
↓
返回 tool_calls
↓
后端执行工具
↓
得到工具结果
↓
再次发送给模型
↓
模型生成最终回答
```

---

## 今天任务

---

### 1. 新建学习脚本

```text
app/playground/day29_tool_call_concept.py
```

内容：

```python
def simulate_tool_call(user_message: str):
    print(f"用户输入: {user_message}")

    if "订单" in user_message:
        tool_call = {
            "name": "query_order",
            "arguments": {
                "order_id": "1001"
            }
        }

        print("LLM 决定调用工具:")
        print(tool_call)

        result = {
            "order_id": "1001",
            "status": "已发货"
        }

        print("工具执行结果:")
        print(result)

        final_answer = f"订单 {result['order_id']} 当前状态是：{result['status']}"

        print("最终回答:")
        print(final_answer)


simulate_tool_call("帮我查订单 1001")
```

运行：

```powershell
python -m app.playground.day29_tool_call_concept
```

---

### 验收标准

你必须能清楚说出：

**Tool Calling 分 2 次模型调用**

第一次：

```text
决定调用什么工具
```

第二次：

```text
基于工具结果生成自然语言回答
```

---

# Day 30：定义 Tool Schema

---

## 新建

```text
app/schemas/tool.py
```

完整内容：

```python
from pydantic import BaseModel
from typing import Any


class ToolParameter(BaseModel):
    type: str
    description: str


class ToolDefinition(BaseModel):
    name: str
    description: str
    parameters: dict[str, ToolParameter]


class ToolCallRequest(BaseModel):
    name: str
    arguments: dict[str, Any]


class ToolResult(BaseModel):
    tool_name: str
    success: bool
    result: Any
```

---

## 测试

```text
app/playground/day30_tool_schema.py
```

```python
from app.schemas.tool import ToolDefinition

tool = ToolDefinition(
    name="query_order",
    description="查询订单状态",
    parameters={
        "order_id": {
            "type": "string",
            "description": "订单号"
        }
    }
)

print(tool.model_dump())
```

运行：

```powershell
python -m app.playground.day30_tool_schema
```

---

# Day 31：实现 Tool Base 抽象层

---

## 新建

```text
app/tools/base.py
```

完整代码：

```python
from abc import ABC, abstractmethod


class BaseTool(ABC):
    name: str
    description: str

    @abstractmethod
    def execute(self, **kwargs):
        pass

    @abstractmethod
    def to_openai_tool(self):
        pass
```

---

## 理解

这是所有工具的统一规范。

以后每个工具都必须实现：

```python
execute()
to_openai_tool()
```

---

# Day 32：实现订单工具

---

## 新建

```text
app/tools/order_tools.py
```

完整内容：

```python
from app.tools.base import BaseTool
from app.services.order_service import query_order


class QueryOrderTool(BaseTool):
    name = "query_order"
    description = "查询订单状态"

    def execute(self, **kwargs):
        order_id = kwargs.get("order_id")
        return query_order(order_id)

    def to_openai_tool(self):
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "order_id": {
                            "type": "string",
                            "description": "订单号"
                        }
                    },
                    "required": ["order_id"]
                }
            }
        }
```

---

## 测试

```text
app/playground/day32_query_order_tool.py
```

```python
from app.tools.order_tools import QueryOrderTool

tool = QueryOrderTool()

print(tool.execute(order_id="1001"))
print(tool.to_openai_tool())
```

---

# Day 33：实现天气工具

---

## 新建

```text
app/tools/weather_tools.py
```

完整内容：

```python
from app.tools.base import BaseTool


class WeatherTool(BaseTool):
    name = "get_weather"
    description = "查询城市天气"

    def execute(self, **kwargs):
        city = kwargs.get("city")

        weather_data = {
            "北京": "晴 26°C",
            "上海": "多云 24°C",
            "深圳": "小雨 29°C"
        }

        return weather_data.get(city, "暂无天气数据")

    def to_openai_tool(self):
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "city": {
                            "type": "string"
                        }
                    },
                    "required": ["city"]
                }
            }
        }
```

---

# Day 34：实现 Tool Registry

---

## 新建

```text
app/tools/registry.py
```

完整代码：

```python
from app.tools.order_tools import QueryOrderTool
from app.tools.weather_tools import WeatherTool


TOOLS = {
    "query_order": QueryOrderTool(),
    "get_weather": WeatherTool(),
}


def get_tool(name: str):
    return TOOLS.get(name)


def get_openai_tools():
    return [
        tool.to_openai_tool()
        for tool in TOOLS.values()
    ]
```

---

测试：

```python
from app.tools.registry import get_openai_tools

print(get_openai_tools())
```

---

# Day 35：DeepSeek Tool Calling

---

## 新建

```text
app/services/tool_call_service.py
```

完整内容：

```python
import json
from openai import OpenAI

from app.core.config import settings
from app.tools.registry import get_openai_tools

client = OpenAI(
    api_key=settings.deepseek_api_key,
    base_url="https://api.deepseek.com",
)


def request_tool_call(message: str):
    response = client.chat.completions.create(
        model=settings.deepseek_model,
        messages=[
            {
                "role": "user",
                "content": message
            }
        ],
        tools=get_openai_tools(),
        tool_choice="auto"
    )

    return response
```

---

测试：

```text
app/playground/day35_tool_call.py
```

```python
from app.services.tool_call_service import request_tool_call

response = request_tool_call("帮我查订单 1001")

print(response)
```

---

验收：

响应里出现：

```text
tool_calls
```

---

# Day 36：解析 Tool Calls

---

## 新建

```text
app/services/tool_executor.py
```

完整代码：

```python
import json

from app.tools.registry import get_tool


def execute_tool_calls(tool_calls):
    results = []

    for call in tool_calls:
        name = call.function.name
        arguments = json.loads(call.function.arguments)

        tool = get_tool(name)

        if tool:
            result = tool.execute(**arguments)

            results.append({
                "tool_name": name,
                "result": result
            })

    return results
```

---

# Day 37：完整 Agent 闭环

---

## 新建

```text
app/services/tool_agent_service.py
```

完整实现：

```python
import json
from openai import OpenAI

from app.core.config import settings
from app.tools.registry import get_openai_tools
from app.services.tool_executor import execute_tool_calls

client = OpenAI(
    api_key=settings.deepseek_api_key,
    base_url="https://api.deepseek.com",
)


def tool_chat(message: str):
    response = client.chat.completions.create(
        model=settings.deepseek_model,
        messages=[
            {"role": "user", "content": message}
        ],
        tools=get_openai_tools(),
        tool_choice="auto"
    )

    assistant_message = response.choices[0].message

    if not assistant_message.tool_calls:
        return assistant_message.content

    tool_results = execute_tool_calls(
        assistant_message.tool_calls
    )

    final_response = client.chat.completions.create(
        model=settings.deepseek_model,
        messages=[
            {"role": "user", "content": message},
            assistant_message.model_dump(),
            {
                "role": "tool",
                "content": json.dumps(tool_results)
            }
        ]
    )

    return final_response.choices[0].message.content
```

---

# Day 38：新增 Tool Chat API

---

## 新建

```text
app/api/tool_chat.py
```

```python
from fastapi import APIRouter
from app.schemas.chat import ChatRequest, ChatResponse
from app.services.tool_agent_service import tool_chat

router = APIRouter()


@router.post("", response_model=ChatResponse)
def chat(request: ChatRequest):
    reply = tool_chat(request.message)
    return ChatResponse(reply=reply)
```

---

修改：

```python
app/main.py
```

增加：

```python
app.include_router(
    tool_chat.router,
    prefix="/tool-chat",
    tags=["Tool Chat"]
)
```

# Day 39：退款工具（Refund Tool）

今天目标：

```text id="egvztj"
1. 新增退款工具
2. 支持申请退款
3. 支持查询退款状态
4. 接入 Tool Calling
5. 支持自动工具选择
```

---

# 一、项目结构新增

新增：

```text id="8hskfj"
app/
├── services/
│   ├── refund_service.py
├── tools/
│   ├── refund_tools.py
```

---

# 二、实现退款业务层

新建：

```text id="2y0e5o"
app/services/refund_service.py
```

完整代码：

```python id="58z2lb"
refund_db = {
    "R1001": {
        "refund_id": "R1001",
        "order_id": "1001",
        "status": "退款中"
    }
}


def create_refund(order_id: str):
    refund_id = f"R{order_id}"

    refund = {
        "refund_id": refund_id,
        "order_id": order_id,
        "status": "退款申请成功"
    }

    refund_db[refund_id] = refund

    return refund


def query_refund(refund_id: str):
    return refund_db.get(
        refund_id,
        {
            "error": "退款单不存在"
        }
    )
```

---

# 三、实现退款工具

新建：

```text id="ktjlwm"
app/tools/refund_tools.py
```

完整代码：

```python id="egjlwm"
from app.tools.base import BaseTool
from app.services.refund_service import (
    create_refund,
    query_refund
)


class RefundOrderTool(BaseTool):
    name = "refund_order"
    description = "申请订单退款"

    def execute(self, **kwargs):
        order_id = kwargs.get("order_id")

        return create_refund(order_id)

    def to_openai_tool(self):
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "order_id": {
                            "type": "string",
                            "description": "订单号"
                        }
                    },
                    "required": ["order_id"]
                }
            }
        }


class QueryRefundTool(BaseTool):
    name = "query_refund"
    description = "查询退款状态"

    def execute(self, **kwargs):
        refund_id = kwargs.get("refund_id")

        return query_refund(refund_id)

    def to_openai_tool(self):
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "refund_id": {
                            "type": "string",
                            "description": "退款单号"
                        }
                    },
                    "required": ["refund_id"]
                }
            }
        }
```

---

# 四、注册工具

修改：

```text id="mjlwm3"
app/tools/registry.py
```

完整代码：

```python id="qjlwm7"
from app.tools.order_tools import QueryOrderTool
from app.tools.weather_tools import WeatherTool
from app.tools.refund_tools import (
    RefundOrderTool,
    QueryRefundTool
)

TOOLS = {
    "query_order": QueryOrderTool(),
    "get_weather": WeatherTool(),
    "refund_order": RefundOrderTool(),
    "query_refund": QueryRefundTool(),
}


def get_tool(name: str):
    return TOOLS.get(name)


def get_openai_tools():
    return [
        tool.to_openai_tool()
        for tool in TOOLS.values()
    ]
```

---

# 五、测试退款工具

新建：

```text id="jlwm7q"
app/playground/day39_refund_tool.py
```

完整代码：

```python id="jlwm7r"
from app.tools.refund_tools import (
    RefundOrderTool,
    QueryRefundTool
)

refund_tool = RefundOrderTool()

result = refund_tool.execute(
    order_id="1002"
)

print("申请退款:")
print(result)

query_tool = QueryRefundTool()

query_result = query_tool.execute(
    refund_id="R1002"
)

print("查询退款:")
print(query_result)
```

运行：

```powershell id="jlwm7s"
python -m app.playground.day39_refund_tool
```

---

# 六、测试 Tool Calling

启动：

```powershell id="jlwm7t"
python -m uvicorn app.main:app --reload
```

Swagger：

```text id="jlwm7u"
http://127.0.0.1:8000/docs
```

测试：

```json id="jlwm7v"
{
  "message": "帮我申请订单1002退款"
}
```

再测试：

```json id="jlwm7w"
{
  "message": "帮我查询退款单R1002状态"
}
```

---

# 七、今天你必须理解

---

## 1

工具和业务逻辑分层：

```text id="jlwm7x"
tool
↓
service
```

不要：

```text id="jlwm7y"
tool 里直接写复杂业务
```

---

## 2

Tool 只是：

```text id="jlwm7z"
Agent 调用业务的入口
```

真正逻辑：

```text id="jlwm80"
service 层
```

---

# Day 40：多工具路由（Multi Tool Routing）

今天目标：

```text id="jlwm81"
1. 支持多个工具同时调用
2. 理解模型自动路由
3. 理解 multi-tool agent
```

---

# 一、理解多工具调用

用户：

```text id="jlwm82"
帮我查订单1001，然后看看北京天气
```

模型可能返回：

```json id="jlwm83"
[
  {
    "function": {
      "name": "query_order"
    }
  },
  {
    "function": {
      "name": "get_weather"
    }
  }
]
```

说明：

```text id="jlwm84"
模型决定同时调用多个工具
```

---

# 二、修改 tool_executor.py

修改：

```text id="jlwm85"
app/services/tool_executor.py
```

完整代码：

```python id="jlwm86"
import json

from app.tools.registry import get_tool


def execute_tool_calls(tool_calls):
    results = []

    for call in tool_calls:
        name = call.function.name

        arguments = json.loads(
            call.function.arguments
        )

        tool = get_tool(name)

        if not tool:
            results.append({
                "tool_name": name,
                "success": False,
                "error": "工具不存在"
            })

            continue

        try:
            result = tool.execute(
                **arguments
            )

            results.append({
                "tool_name": name,
                "success": True,
                "result": result
            })

        except Exception as error:
            results.append({
                "tool_name": name,
                "success": False,
                "error": str(error)
            })

    return results
```

---

# 三、修改 tool_agent_service.py

修改：

```python id="jlwm87"
for tool_call, tool_result in zip(
    assistant_message.tool_calls,
    tool_results
):
```

下面增加日志：

```python id="jlwm88"
print("工具调用:")
print(tool_call.function.name)

print("工具结果:")
print(tool_result)
```

---

# 四、测试多工具

请求：

```json id="jlwm89"
{
  "message": "帮我查订单1001，然后看看北京天气"
}
```

观察终端：

```text id="jlwm8a"
工具调用:
query_order

工具调用:
get_weather
```

---

# 五、今天核心理解

---

## Tool Calling 不一定只有一个工具

真正 Agent：

```text id="jlwm8b"
可以一次规划多个工具
```

这就是：

```text id="jlwm8c"
Multi-step reasoning
```

---

# Day 41：工具异常处理 + Retry

今天目标：

```text id="jlwm8d"
1. 工具失败不崩溃
2. 工具重试
3. Agent 容错
```

---

# 一、修改天气工具（模拟异常）

修改：

```text id="jlwm8e"
app/tools/weather_tools.py
```

增加：

```python id="jlwm8f"
if city == "火星":
    raise Exception("天气服务异常")
```

完整 execute：

```python id="jlwm8g"
def execute(self, **kwargs):
    city = kwargs.get("city")

    if city == "火星":
        raise Exception("天气服务异常")

    weather_data = {
        "北京": "晴 26°C",
        "上海": "多云 24°C",
        "深圳": "小雨 29°C"
    }

    return weather_data.get(
        city,
        "暂无天气数据"
    )
```

---

# 二、增加工具重试机制

修改：

```text id="jlwm8h"
app/services/tool_executor.py
```

完整代码：

```python id="jlwm8i"
import json
import time

from app.tools.registry import get_tool


def execute_single_tool(
    tool,
    arguments,
    retry=2
):
    for attempt in range(retry):
        try:
            return {
                "success": True,
                "result": tool.execute(
                    **arguments
                )
            }

        except Exception as error:
            print(f"工具执行失败: {error}")

            if attempt == retry - 1:
                return {
                    "success": False,
                    "error": str(error)
                }

            time.sleep(1)

    return {
        "success": False,
        "error": "未知错误"
    }


def execute_tool_calls(tool_calls):
    results = []

    for call in tool_calls:
        name = call.function.name

        arguments = json.loads(
            call.function.arguments
        )

        tool = get_tool(name)

        if not tool:
            results.append({
                "tool_name": name,
                "success": False,
                "error": "工具不存在"
            })

            continue

        result = execute_single_tool(
            tool,
            arguments
        )

        results.append({
            "tool_name": name,
            **result
        })

    return results
```

---

# 三、测试异常

请求：

```json id="jlwm8j"
{
  "message": "帮我查询火星天气"
}
```

观察终端：

```text id="jlwm8k"
工具执行失败
重试
```

最终模型应该返回：

```text id="jlwm8l"
天气服务暂时不可用
```

---

# 四、今天必须理解

---

## Agent 最大难点之一：

```text id="jlwm8m"
工具永远可能失败
```

例如：

```text id="jlwm8n"
数据库挂了
HTTP 超时
第三方 API 限流
```

所以：

```text id="jlwm8o"
真正 Agent Runtime
必须有 retry
```

---

# Day 42：阶段三最终验收

今天目标：

```text id="jlwm8p"
1. 全链路测试
2. pytest
3. README
4. 阶段总结
```

---

# 一、测试所有 Tool Chat

测试：

---

## 订单查询

```json id="jlwm8q"
{
  "message": "帮我查订单1001"
}
```

---

## 天气查询

```json id="jlwm8r"
{
  "message": "北京天气怎么样"
}
```

---

## 退款申请

```json id="jlwm8s"
{
  "message": "帮我申请订单1002退款"
}
```

---

## 多工具

```json id="jlwm8t"
{
  "message": "帮我查订单1001，再看看北京天气"
}
```

---

# 二、新增 pytest

新增：

```text id="jlwm8u"
tests/test_tool_executor.py
```

完整代码：

```python id="jlwm8v"
from app.services.tool_executor import (
    execute_single_tool
)

from app.tools.weather_tools import (
    WeatherTool
)


def test_weather_tool_success():
    tool = WeatherTool()

    result = execute_single_tool(
        tool,
        {
            "city": "北京"
        }
    )

    assert result["success"] is True


def test_weather_tool_fail():
    tool = WeatherTool()

    result = execute_single_tool(
        tool,
        {
            "city": "火星"
        }
    )

    assert result["success"] is False
```

---

新增：

```text id="jlwm8w"
tests/test_refund_service.py
```

完整代码：

```python id="jlwm8x"
from app.services.refund_service import (
    create_refund,
    query_refund
)


def test_create_refund():
    result = create_refund(
        "1005"
    )

    assert result["refund_id"] == "R1005"


def test_query_refund():
    result = query_refund(
        "R1001"
    )

    assert result["refund_id"] == "R1001"
```

---

# 三、运行测试

```powershell id="jlwm8y"
pytest
```

---

# 四、README 更新

新增：

````markdown id="jlwm8z"
## 阶段三：Tool Calling

当前系统支持：

- 自动订单查询
- 自动天气查询
- 自动退款申请
- 自动退款状态查询
- 多工具自动路由
- Tool Calling
- 工具失败重试

## Tool Chat API

POST /tool-chat

请求：

```json
{
  "message": "帮我查订单1001"
}
````

返回：

```json
{
  "reply": "订单1001当前状态为已发货"
}
```

````

---

# 五、阶段三最终你必须彻底掌握

---

## 1

什么是：

```text id="jlwm90"
Tool Calling
````

---

## 2

为什么需要：

```text id="jlwm91"
tool schema
tool registry
tool executor
```

---

## 3

为什么：

```text id="jlwm92"
assistant_message.model_dump()
```

必须传回模型

---

## 4

为什么：

```text id="jlwm93"
tool_call_id
```

必须存在

---

## 5

为什么：

```text id="jlwm94"
tool_result
```

必须和：

```text id="jlwm95"
tool_call
```

一一对应

---

## 6

为什么：

```text id="jlwm96"
真正 Agent Runtime
一定要做 retry
```

---

# 阶段三完成后

你已经不是：

```text id="jlwm97"
普通 AI 聊天
```

而是真正开始进入：

```text id="jlwm98"
Agent Runtime
```

下一阶段：

```text id="jlwm99"
阶段4：Memory + RAG + 长期记忆
Day 43 - Day 56
```

你会开始实现：

```text id="jlwm9a"
向量数据库
Embedding
RAG
长期记忆
知识检索
```
