下面把 **Day 21 - Day 28：第二阶段剩余任务**补充成可直接照做的完整版本。

下面默认你现在使用的是 **DeepSeek + OpenAI Python SDK 兼容模式**，所以代码统一使用：

```python
client.chat.completions.create(...)
```

不使用：

```python
client.responses.create(...)
client.responses.parse(...)
```

---

# 开始前确认配置

先确认你的 `.env` 是这种形式：

```env
API_KEY=你的_deepseek_key
MODEL=deepseek-chat
```

或者你实际可用的模型名，例如：

```env
MODEL=deepseek-reasoner
```

确认 `app/core/config.py`：

```python
import os
from dotenv import load_dotenv

load_dotenv()


class Settings:
    API_KEY: str | None = os.getenv("API_KEY")
    MODEL: str = os.getenv("MODEL", "deepseek-chat")


settings = Settings()
```

确认 `app/services/llm_service.py` 当前基础版本类似这样：

```python
from openai import OpenAI
from app.core.config import settings

client = OpenAI(
    api_key=settings.API_KEY,
    base_url="https://api.deepseek.com",
)


def ask_llm(prompt: str) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {"role": "system", "content": "你是一个 Python Agent 开发学习助手。"},
            {"role": "user", "content": prompt},
        ],
        stream=False,
    )

    return response.choices[0].message.content
```

---

# Day 21：面试题 Schema 设计

## 今日目标

增加面试助手相关的数据结构，为后续接口做准备。

你今天要完成：

```text
1. 定义生成面试题请求结构
2. 定义面试题响应结构
3. 定义回答评分请求结构
4. 定义回答评分响应结构
5. 用 Python 命令验证 Schema 可用
```

---

## 新建文件

```text
app/schemas/interview.py
```

完整内容：

```python
from pydantic import BaseModel, Field


class InterviewQuestionRequest(BaseModel):
    resume: str = Field(description="候选人的简历内容")
    job: str = Field(description="目标岗位，例如：前端开发工程师")
    count: int = Field(default=5, description="生成面试题数量")


class InterviewQuestion(BaseModel):
    question: str = Field(description="面试问题")
    difficulty: str = Field(description="难度，例如：简单、中等、困难")
    point: str = Field(description="考察点")
    reference_answer: str = Field(description="参考答案")


class InterviewQuestionResponse(BaseModel):
    questions: list[InterviewQuestion]


class EvaluateAnswerRequest(BaseModel):
    question: str = Field(description="面试问题")
    answer: str = Field(description="候选人的回答")


class EvaluateAnswerResponse(BaseModel):
    score: int = Field(description="评分，0 到 100")
    strengths: list[str] = Field(description="回答中的优点")
    weaknesses: list[str] = Field(description="回答中的不足")
    improved_answer: str = Field(description="优化后的参考回答")
```

---

## 新建测试脚本

```text
app/playground/day21_interview_schema.py
```

内容：

```python
from app.schemas.interview import (
    InterviewQuestionRequest,
    InterviewQuestionResponse,
    InterviewQuestion,
    EvaluateAnswerRequest,
    EvaluateAnswerResponse,
)

request = InterviewQuestionRequest(
    resume="熟悉 React、Vue、TypeScript，有 AI 辅助研发经验。",
    job="前端开发工程师",
    count=3,
)

print(request.model_dump())

response = InterviewQuestionResponse(
    questions=[
        InterviewQuestion(
            question="你在项目中如何使用 AI 编程工具提升研发效率？",
            difficulty="中等",
            point="考察 AI 辅助研发理解和实践能力",
            reference_answer="可以从 Prompt 设计、代码生成、代码 Review、测试用例生成等方面回答。",
        )
    ]
)

print(response.model_dump())

evaluate_request = EvaluateAnswerRequest(
    question="你如何理解 React 组件化？",
    answer="我会把页面拆成多个组件。",
)

print(evaluate_request.model_dump())

evaluate_response = EvaluateAnswerResponse(
    score=80,
    strengths=["回答方向正确", "能体现基础理解"],
    weaknesses=["缺少复杂业务场景", "缺少工程化细节"],
    improved_answer="React 组件化是将页面拆分为可复用、可维护的 UI 单元...",
)

print(evaluate_response.model_dump())
```

---

## 运行

在项目根目录执行：

```powershell
python -m app.playground.day21_interview_schema
```

---

## 验收标准

终端能正常输出 4 段结构化数据，不报错。

你要能说清楚：

```text
InterviewQuestionRequest：生成面试题请求
InterviewQuestionResponse：生成面试题响应
EvaluateAnswerRequest：回答评分请求
EvaluateAnswerResponse：回答评分响应
```

---

# Day 22：面试题 Prompt 和 Service

## 今日目标

实现两个核心能力：

```text
1. 根据简历和目标岗位生成面试题
2. 根据问题和回答进行评分
```

由于 DeepSeek 不支持 `responses.parse()`，所以这里使用：

```text
模型输出 JSON 字符串
↓
json.loads()
↓
Pydantic 校验
```

---

## 修改 `app/services/prompt_service.py`

在原有内容后面追加：

````python
def build_interview_questions_prompt(resume: str, job: str, count: int) -> str:
    return f"""
你是一名资深技术面试官。

请根据候选人的简历和目标岗位，生成 {count} 个面试问题。

要求：
1. 问题必须贴合简历内容
2. 覆盖技术基础、项目经验、工程能力、AI 辅助研发
3. 每个问题包含：问题、难度、考察点、参考答案
4. 不要编造简历中没有的项目经历
5. 必须只返回 JSON，不要返回 Markdown，不要使用 ```json 包裹

返回 JSON 格式必须是：
{{
  "questions": [
    {{
      "question": "问题内容",
      "difficulty": "简单/中等/困难",
      "point": "考察点",
      "reference_answer": "参考答案"
    }}
  ]
}}

候选人简历：
{resume}

目标岗位：
{job}
"""


def build_evaluate_answer_prompt(question: str, answer: str) -> str:
    return f"""
你是一名技术面试官。

请评估候选人对面试问题的回答。

评分标准：
1. 技术准确性
2. 表达清晰度
3. 是否结合项目经验
4. 是否体现工程化思维
5. 是否有 AI 辅助研发理解，若问题相关

要求：
1. 分数范围是 0 到 100
2. strengths 至少 2 条
3. weaknesses 至少 2 条
4. improved_answer 要像真实面试参考答案
5. 必须只返回 JSON，不要返回 Markdown，不要使用 ```json 包裹

返回 JSON 格式必须是：
{{
  "score": 80,
  "strengths": ["优点1", "优点2"],
  "weaknesses": ["不足1", "不足2"],
  "improved_answer": "优化后的参考回答"
}}

面试问题：
{question}

候选人回答：
{answer}
"""
````

---

## 新建文件

```text
app/services/interview_service.py
```

完整内容：

````python
import json
from openai import OpenAI
from pydantic import ValidationError

from app.core.config import settings
from app.core.logger import logger
from app.schemas.interview import (
    InterviewQuestionResponse,
    EvaluateAnswerResponse,
)
from app.services.prompt_service import (
    build_interview_questions_prompt,
    build_evaluate_answer_prompt,
)

client = OpenAI(
    api_key=settings.API_KEY,
    base_url="https://api.deepseek.com",
)


def _load_json(content: str) -> dict:
    """
    将模型返回内容转换为 dict。
    理论上 response_format=json_object 会返回纯 JSON，
    这里做一层兼容，避免模型偶尔返回 Markdown 包裹。
    """
    content = content.strip()

    if content.startswith("```json"):
        content = content.removeprefix("```json").removesuffix("```").strip()

    if content.startswith("```"):
        content = content.removeprefix("```").removesuffix("```").strip()

    return json.loads(content)


def generate_interview_questions(
    resume: str,
    job: str,
    count: int,
) -> InterviewQuestionResponse:
    prompt = build_interview_questions_prompt(
        resume=resume,
        job=job,
        count=count,
    )

    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {
                "role": "system",
                "content": "你是一个严格输出 JSON 的技术面试官。",
            },
            {
                "role": "user",
                "content": prompt,
            },
        ],
        response_format={"type": "json_object"},
        stream=False,
    )

    content = response.choices[0].message.content

    try:
        data = _load_json(content)
        return InterviewQuestionResponse(**data)
    except (json.JSONDecodeError, ValidationError) as error:
        logger.exception("生成面试题结构化解析失败")
        raise ValueError(f"生成面试题失败：{error}") from error


def evaluate_answer(
    question: str,
    answer: str,
) -> EvaluateAnswerResponse:
    prompt = build_evaluate_answer_prompt(
        question=question,
        answer=answer,
    )

    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {
                "role": "system",
                "content": "你是一个严格输出 JSON 的技术面试官。",
            },
            {
                "role": "user",
                "content": prompt,
            },
        ],
        response_format={"type": "json_object"},
        stream=False,
    )

    content = response.choices[0].message.content

    try:
        data = _load_json(content)
        return EvaluateAnswerResponse(**data)
    except (json.JSONDecodeError, ValidationError) as error:
        logger.exception("面试回答评分结构化解析失败")
        raise ValueError(f"面试回答评分失败：{error}") from error
````

---

## 新建测试脚本

```text
app/playground/day22_interview_service.py
```

内容：

```python
from app.services.interview_service import (
    generate_interview_questions,
    evaluate_answer,
)

resume = """
前端开发工程师，熟悉 React、Vue、TypeScript。
参与过后台管理系统、动态表单、数据可视化项目。
熟练使用 Claude Code、Codex 等 AI 编程工具，
能够通过结构化 Prompt 进行代码生成、组件逻辑补全、代码 Review 和测试用例生成。
"""

result = generate_interview_questions(
    resume=resume,
    job="前端开发工程师",
    count=3,
)

print("生成面试题：")
print(result.model_dump())

evaluate_result = evaluate_answer(
    question="你在项目中如何使用 AI 工具提升研发效率？",
    answer="我会用 AI 帮我生成代码，也会让它帮我检查问题。",
)

print("回答评分：")
print(evaluate_result.model_dump())
```

---

## 运行

```powershell
python -m app.playground.day22_interview_service
```

---

## 验收标准

终端输出中必须有：

```text
questions
question
difficulty
point
reference_answer
```

以及：

```text
score
strengths
weaknesses
improved_answer
```

---

# Day 23：面试题 API

## 今日目标

新增两个接口：

```text
POST /interview/questions
POST /interview/evaluate
```

---

## 新建文件

```text
app/api/interview.py
```

完整内容：

```python
from fastapi import APIRouter, HTTPException

from app.schemas.interview import (
    InterviewQuestionRequest,
    InterviewQuestionResponse,
    EvaluateAnswerRequest,
    EvaluateAnswerResponse,
)
from app.services.interview_service import (
    generate_interview_questions,
    evaluate_answer,
)

router = APIRouter()


@router.post("/questions", response_model=InterviewQuestionResponse)
def create_questions(request: InterviewQuestionRequest):
    try:
        return generate_interview_questions(
            resume=request.resume,
            job=request.job,
            count=request.count,
        )
    except Exception as error:
        raise HTTPException(
            status_code=500,
            detail=str(error),
        )


@router.post("/evaluate", response_model=EvaluateAnswerResponse)
def evaluate(request: EvaluateAnswerRequest):
    try:
        return evaluate_answer(
            question=request.question,
            answer=request.answer,
        )
    except Exception as error:
        raise HTTPException(
            status_code=500,
            detail=str(error),
        )
```

---

## 修改 `app/main.py`

确认导入里包含 `interview`：

```python
from fastapi import FastAPI
from app.api import order, chat, agent, interview

app = FastAPI(title="AI Agent Learning Backend")

app.include_router(order.router, prefix="/orders", tags=["Orders"])
app.include_router(chat.router, prefix="/chat", tags=["Chat"])
app.include_router(agent.router, prefix="/agent", tags=["Agent"])
app.include_router(interview.router, prefix="/interview", tags=["Interview"])


@app.get("/health")
def health_check():
    return {"status": "ok"}
```

---

## 启动项目

```powershell
python -m uvicorn app.main:app --reload
```

打开：

```text
http://127.0.0.1:8000/docs
```

---

## 测试 `/interview/questions`

请求：

```json
{
  "resume": "熟悉 React、Vue、TypeScript，有后台管理系统和 AI 辅助研发经验，使用过 Claude Code 和 Codex。",
  "job": "前端开发工程师",
  "count": 3
}
```

预期返回：

```json
{
  "questions": [
    {
      "question": "...",
      "difficulty": "...",
      "point": "...",
      "reference_answer": "..."
    }
  ]
}
```

---

## 测试 `/interview/evaluate`

请求：

```json
{
  "question": "你如何使用 AI 工具提升前端研发效率？",
  "answer": "我会用 AI 生成代码、补全组件逻辑，也会让它帮我做代码 Review。"
}
```

预期返回：

```json
{
  "score": 80,
  "strengths": [],
  "weaknesses": [],
  "improved_answer": "..."
}
```

---

## 验收标准

Swagger 里出现：

```text
POST /interview/questions
POST /interview/evaluate
```

并且两个接口都能正常返回结构化 JSON。

---

# Day 24：多轮对话记忆

## 今日目标

让 `/chat` 支持简单多轮上下文。

你要实现：

```text
1. conversation_id
2. 保存 user / assistant 历史消息
3. 每次请求带上历史消息
4. 限制最多保留最近 10 轮，避免上下文太长
```

---

## 新建文件

```text
app/services/conversation_service.py
```

完整内容：

```python
conversation_store: dict[str, list[dict]] = {}


def add_message(conversation_id: str, role: str, content: str):
    if conversation_id not in conversation_store:
        conversation_store[conversation_id] = []

    conversation_store[conversation_id].append({
        "role": role,
        "content": content,
    })

    # 最多保留最近 20 条消息，也就是大约 10 轮对话
    conversation_store[conversation_id] = conversation_store[conversation_id][-20:]


def get_messages(conversation_id: str) -> list[dict]:
    return conversation_store.get(conversation_id, [])


def clear_messages(conversation_id: str):
    conversation_store.pop(conversation_id, None)
```

---

## 修改 `app/schemas/chat.py`

完整内容改成：

```python
from pydantic import BaseModel


class ChatRequest(BaseModel):
    message: str
    conversation_id: str = "default"


class ChatResponse(BaseModel):
    reply: str
```

---

## 修改 `app/services/llm_service.py`

追加一个基于 messages 的方法：

```python
def ask_llm_messages(messages: list[dict]) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=messages,
        stream=False,
    )

    return response.choices[0].message.content
```

此时 `llm_service.py` 应该至少有：

```python
from openai import OpenAI
from app.core.config import settings

client = OpenAI(
    api_key=settings.API_KEY,
    base_url="https://api.deepseek.com",
)


def ask_llm(prompt: str) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {"role": "system", "content": "你是一个 Python Agent 开发学习助手。"},
            {"role": "user", "content": prompt},
        ],
        stream=False,
    )

    return response.choices[0].message.content


def ask_llm_messages(messages: list[dict]) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=messages,
        stream=False,
    )

    return response.choices[0].message.content
```

---

## 修改 `app/services/chat_service.py`

完整内容改成：

```python
from app.services.llm_service import ask_llm_messages
from app.services.conversation_service import add_message, get_messages


def simple_chat(message: str, conversation_id: str = "default") -> str:
    add_message(conversation_id, "user", message)

    messages = [
        {
            "role": "system",
            "content": "你是一个 Python Agent 开发学习助手。请用中文回答，适合初学者理解。",
        },
        *get_messages(conversation_id),
    ]

    reply = ask_llm_messages(messages)

    add_message(conversation_id, "assistant", reply)

    return reply
```

---

## 修改 `app/api/chat.py`

完整内容参考：

```python
from fastapi import APIRouter
from app.schemas.chat import ChatRequest, ChatResponse
from app.services.chat_service import simple_chat

router = APIRouter()


@router.post("", response_model=ChatResponse)
def chat(request: ChatRequest):
    reply = simple_chat(
        message=request.message,
        conversation_id=request.conversation_id,
    )
    return ChatResponse(reply=reply)
```

---

## 测试方式

第一次请求：

```json
{
  "conversation_id": "conv_001",
  "message": "什么是 FastAPI？"
}
```

第二次请求：

```json
{
  "conversation_id": "conv_001",
  "message": "那它和 Flask 有什么区别？"
}
```

---

## 验收标准

第二次回答能理解“它”指的是 FastAPI。

---

# Day 25：流式 LLM Service

## 今日目标

实现流式输出能力，为后续前端打字机效果做准备。

---

## 修改 `app/services/llm_service.py`

追加：

```python
def stream_llm(prompt: str):
    stream = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {
                "role": "system",
                "content": "你是一个 Python Agent 开发学习助手。请用中文回答。",
            },
            {
                "role": "user",
                "content": prompt,
            },
        ],
        stream=True,
    )

    for chunk in stream:
        delta = chunk.choices[0].delta.content
        if delta:
            yield delta
```

---

## 新建测试脚本

```text
app/playground/day25_stream_test.py
```

内容：

```python
from app.services.llm_service import stream_llm

for chunk in stream_llm("请解释 RAG 的完整流程"):
    print(chunk, end="", flush=True)
```

---

## 运行

```powershell
python -m app.playground.day25_stream_test
```

---

## 验收标准

终端不是一次性输出，而是一段一段输出内容。

如果没有流式效果，但最终能输出，也先算通过；后续前端联调时再优化体验。

---

# Day 26：流式接口 `/chat/stream`

## 今日目标

新增 SSE 流式接口：

```text
POST /chat/stream
```

---

## 修改 `app/api/chat.py`

完整内容改成：

```python
from fastapi import APIRouter
from fastapi.responses import StreamingResponse

from app.schemas.chat import ChatRequest, ChatResponse
from app.services.chat_service import simple_chat
from app.services.llm_service import stream_llm

router = APIRouter()


@router.post("", response_model=ChatResponse)
def chat(request: ChatRequest):
    reply = simple_chat(
        message=request.message,
        conversation_id=request.conversation_id,
    )
    return ChatResponse(reply=reply)


@router.post("/stream")
def chat_stream(request: ChatRequest):
    def event_generator():
        for chunk in stream_llm(request.message):
            yield f"data: {chunk}\n\n"

        yield "data: [DONE]\n\n"

    return StreamingResponse(
        event_generator(),
        media_type="text/event-stream",
    )
```

---

## 启动服务

```powershell
python -m uvicorn app.main:app --reload
```

---

## 测试方式一：Swagger

Swagger 对 SSE 支持一般，可能看不到很好的流式效果，但可以先确认接口不报错。

---

## 测试方式二：PowerShell 使用 curl.exe

注意 Windows PowerShell 里的 `curl` 可能是别名，所以建议用：

```powershell
curl.exe -N -X POST "http://127.0.0.1:8000/chat/stream" ^
  -H "Content-Type: application/json" ^
  -d "{\"message\":\"请解释什么是 Tool Calling\",\"conversation_id\":\"conv_stream\"}"
```

PowerShell 单行版本：

```powershell
curl.exe -N -X POST "http://127.0.0.1:8000/chat/stream" -H "Content-Type: application/json" -d "{\"message\":\"请解释什么是 Tool Calling\",\"conversation_id\":\"conv_stream\"}"
```

---

## 验收标准

终端能看到类似：

```text
data: Tool Calling 是...
data: ...
data: [DONE]
```

---

# Day 27：LLM 异常处理和重试

## 今日目标

避免模型调用失败时接口直接崩溃。

你要实现：

```text
1. ask_llm_safe()
2. ask_llm_messages_safe()
3. stream_llm 异常兜底
4. chat_service 使用 safe 版本
```

---

## 修改 `app/services/llm_service.py`

建议整理成下面完整版本：

```python
import time
import random
from openai import OpenAI

from app.core.config import settings
from app.core.logger import logger

client = OpenAI(
    api_key=settings.API_KEY,
    base_url="https://api.deepseek.com",
)


def ask_llm(prompt: str) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=[
            {"role": "system", "content": "你是一个 Python Agent 开发学习助手。"},
            {"role": "user", "content": prompt},
        ],
        stream=False,
    )

    return response.choices[0].message.content


def ask_llm_messages(messages: list[dict]) -> str:
    response = client.chat.completions.create(
        model=settings.MODEL,
        messages=messages,
        stream=False,
    )

    return response.choices[0].message.content


def ask_llm_safe(prompt: str, max_retries: int = 3) -> str:
    for attempt in range(max_retries):
        try:
            return ask_llm(prompt)
        except Exception as error:
            logger.exception(f"LLM 调用失败，第 {attempt + 1} 次")

            if attempt == max_retries - 1:
                return "抱歉，AI 服务暂时不可用，请稍后再试。"

            sleep_seconds = 1 + random.random()
            time.sleep(sleep_seconds)

    return "抱歉，AI 服务暂时不可用，请稍后再试。"


def ask_llm_messages_safe(messages: list[dict], max_retries: int = 3) -> str:
    for attempt in range(max_retries):
        try:
            return ask_llm_messages(messages)
        except Exception as error:
            logger.exception(f"LLM 多轮对话调用失败，第 {attempt + 1} 次")

            if attempt == max_retries - 1:
                return "抱歉，AI 服务暂时不可用，请稍后再试。"

            sleep_seconds = 1 + random.random()
            time.sleep(sleep_seconds)

    return "抱歉，AI 服务暂时不可用，请稍后再试。"


def stream_llm(prompt: str):
    try:
        stream = client.chat.completions.create(
            model=settings.MODEL,
            messages=[
                {
                    "role": "system",
                    "content": "你是一个 Python Agent 开发学习助手。请用中文回答。",
                },
                {
                    "role": "user",
                    "content": prompt,
                },
            ],
            stream=True,
        )

        for chunk in stream:
            delta = chunk.choices[0].delta.content
            if delta:
                yield delta

    except Exception as error:
        logger.exception("LLM 流式调用失败")
        yield "抱歉，AI 服务暂时不可用，请稍后再试。"
```

---

## 修改 `app/services/chat_service.py`

把：

```python
from app.services.llm_service import ask_llm_messages
```

改成：

```python
from app.services.llm_service import ask_llm_messages_safe
```

完整内容：

```python
from app.services.llm_service import ask_llm_messages_safe
from app.services.conversation_service import add_message, get_messages


def simple_chat(message: str, conversation_id: str = "default") -> str:
    add_message(conversation_id, "user", message)

    messages = [
        {
            "role": "system",
            "content": "你是一个 Python Agent 开发学习助手。请用中文回答，适合初学者理解。",
        },
        *get_messages(conversation_id),
    ]

    reply = ask_llm_messages_safe(messages)

    add_message(conversation_id, "assistant", reply)

    return reply
```

---

## 验收标准

正常情况下：

```text
/chat 能正常返回
/chat/stream 能正常返回
```

异常情况下：

```text
不会直接暴露 Python 报错堆栈
会返回“抱歉，AI 服务暂时不可用，请稍后再试。”
```

可以临时把 `.env` 里的 API Key 改错测试，测试完记得改回来。

---

# Day 28：第二阶段验收、测试和 README 更新

## 今日目标

整理第二阶段成果，补充测试和文档。

---

## 新建测试文件

```text
tests/test_prompt_service.py
```

内容：

```python
from app.services.prompt_service import (
    build_chat_prompt,
    build_intent_prompt,
    build_interview_questions_prompt,
    build_evaluate_answer_prompt,
)


def test_build_chat_prompt():
    prompt = build_chat_prompt("什么是 Agent？")

    assert "什么是 Agent" in prompt
    assert "中文" in prompt


def test_build_intent_prompt():
    prompt = build_intent_prompt("帮我查订单 1001")

    assert "query_order" in prompt
    assert "订单" in prompt


def test_build_interview_questions_prompt():
    prompt = build_interview_questions_prompt(
        resume="熟悉 React 和 AI 辅助研发",
        job="前端开发工程师",
        count=3,
    )

    assert "3" in prompt
    assert "前端开发工程师" in prompt
    assert "React" in prompt


def test_build_evaluate_answer_prompt():
    prompt = build_evaluate_answer_prompt(
        question="什么是 React 组件化？",
        answer="把页面拆成组件。",
    )

    assert "React 组件化" in prompt
    assert "把页面拆成组件" in prompt
```

---

## 新建测试文件

```text
tests/test_conversation_service.py
```

内容：

```python
from app.services.conversation_service import (
    add_message,
    get_messages,
    clear_messages,
)


def test_add_and_get_messages():
    conversation_id = "test_conv"

    clear_messages(conversation_id)

    add_message(conversation_id, "user", "你好")
    add_message(conversation_id, "assistant", "你好，我是助手")

    messages = get_messages(conversation_id)

    assert len(messages) == 2
    assert messages[0]["role"] == "user"
    assert messages[1]["role"] == "assistant"

    clear_messages(conversation_id)


def test_message_limit():
    conversation_id = "test_limit"

    clear_messages(conversation_id)

    for index in range(30):
        add_message(conversation_id, "user", f"message {index}")

    messages = get_messages(conversation_id)

    assert len(messages) == 20

    clear_messages(conversation_id)
```

---

## 运行测试

```powershell
pytest
```

---

## 更新 README

在 `README.md` 增加第二阶段内容：

````markdown
## 第二阶段功能

当前项目已经接入 DeepSeek LLM API，支持：

- 普通 AI 对话
- Prompt 模板管理
- 结构化意图识别
- 面试题生成
- 面试回答评分
- 多轮对话记忆
- SSE 流式响应
- LLM 调用异常兜底

## 环境变量

```env
API_KEY=你的_deepseek_key
MODEL=deepseek-chat
````

## 启动项目

```bash
python -m uvicorn app.main:app --reload
```

## 测试

```bash
pytest
```

## 当前接口

### POST /chat

普通聊天接口，支持 conversation_id。

请求：

```json
{
  "conversation_id": "conv_001",
  "message": "什么是 FastAPI？"
}
```

### POST /chat/stream

流式聊天接口。

请求：

```json
{
  "conversation_id": "conv_001",
  "message": "请解释什么是 RAG"
}
```

### POST /interview/questions

生成面试题。

请求：

```json
{
  "resume": "熟悉 React、Vue、TypeScript，有 AI 辅助研发经验。",
  "job": "前端开发工程师",
  "count": 3
}
```

### POST /interview/evaluate

面试回答评分。

请求：

```json
{
  "question": "你如何使用 AI 工具提升研发效率？",
  "answer": "我会用 AI 生成代码、补全组件逻辑、做代码 Review。"
}
```

````

---

## 第二阶段最终验收清单

你要逐项确认：

```text
1. python -m uvicorn app.main:app --reload 能启动
2. /docs 能打开
3. /chat 能返回真实 DeepSeek 回答
4. /chat 支持 conversation_id 多轮对话
5. /chat/stream 能流式返回
6. /interview/questions 能生成结构化面试题
7. /interview/evaluate 能返回结构化评分
8. prompt_service.py 统一管理 Prompt
9. llm_service.py 支持普通调用、messages 调用、流式调用、安全调用
10. pytest 全部通过
11. README 已更新第二阶段内容
````

---

# Day 21 - Day 28 完成后的项目结构

完成后，你的项目会新增这些文件：

```text
app/
├── api/
│   ├── chat.py
│   ├── interview.py
├── schemas/
│   ├── chat.py
│   ├── interview.py
├── services/
│   ├── conversation_service.py
│   ├── interview_service.py
│   ├── llm_service.py
│   ├── prompt_service.py
├── playground/
│   ├── day21_interview_schema.py
│   ├── day22_interview_service.py
│   ├── day25_stream_test.py
tests/
├── test_prompt_service.py
├── test_conversation_service.py
```

---

# 第二阶段完成后你应该掌握什么

完成 Day 28 后，你要能说清楚：

```text
1. DeepSeek 为什么用 chat.completions.create，而不是 responses.create
2. Prompt 为什么要单独放到 prompt_service.py
3. 结构化输出为什么要用 JSON + Pydantic 校验
4. 多轮对话为什么要保存 user / assistant 历史消息
5. 流式响应为什么适合前端打字机效果
6. LLM 调用为什么要加异常兜底和重试
7. 面试题生成为什么适合作为一个独立业务模块
```

---

# 下一步进入 Day 29

Day 28 完成后，下一阶段就是：

```text
第三阶段：Tool Calling / Function Calling
```

也就是把现在的：

```text
LLM 识别意图
后端根据意图调用工具
```

升级成：

```text
LLM 自己返回 tool_call
后端解析 tool_call
后端执行工具
再把工具结果交回模型
```

这才是真正的 Agent 工具调用闭环。
